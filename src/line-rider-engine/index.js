export {default} from './LineRiderEngine.js'

/*
export class CustomLineRiderEngine {
  constructor ({legacyGrid}) {
    class CustomLineRiderEngine extends LineRiderEngine {
      makeGrid () {
        if (legacyGrid) return
      }
    }
    return new CustomLineRiderEngine()
  }
}
*/
